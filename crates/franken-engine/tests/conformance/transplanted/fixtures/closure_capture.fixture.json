{
  "donor_harness": "quickjs",
  "source": "var fns = []; for (let i = 0; i < 3; i++) { fns.push(function() { return i; }); } print(fns.map(f => f()).join(','));",
  "observed_output": "0,1,2\nprops:length,0,1,2"
}
