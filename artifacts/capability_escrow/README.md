# Capability Escrow Artifacts

Canonical capability escrow reproducibility bundles are maintained under:

- `artifacts/capability_escrow_suite/`

Use:

```bash
./scripts/run_capability_escrow_suite.sh ci
```

and inspect the latest timestamped bundle there.
